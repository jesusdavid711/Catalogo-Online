package com.example.CatalogoOnline.controller;

/**
 * Preserved file as historical reference during HU3 refactor.
 * The real controller was moved to `com.example.CatalogoOnline.infraestructura.adapters.in.web`.
 * Does not contain active implementation to avoid duplicate bean registration.
 */
public final class VenueController {
    private VenueController() {
        // utility class - no instances
    }
}